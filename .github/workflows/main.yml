name: Cypress Tests

on:
  pull_request_target:
    types:
      - opened
    branches: 
      - main
      - dev-env

jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3
